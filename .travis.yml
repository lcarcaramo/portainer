arch: s390x
services:
  - docker
env:
  global:
    - TEST_DIR="$PWD"
    - TEST_SCRIPT="test.sh"
    - BUILD_DIR="$PWD"
    - REPO="quay.io/ibmz/elasticsearch"
    - TAG="$REPO:7.9.1"
before_script: 
  #- cd $TEST_DIR && chmod +x $TEST_SCRIPT
  # - docker login quay.io -u $ROBOT -p $ROBOT_TOKEN
script: 
  - echo "PWD from travis:" && pwd && ls
  - docker build $PWD --tag portainer/base
  #- docker build $BUILD_DIR -f $BUILD_DIR/alpine.Dockerfile --tag $TAG
  #- cd $TEST_DIR && bash $TEST_SCRIPT 
  #mkdir -p ${GOPATH}/src/github.com/portainer
  #ln -s ${PWD} ${GOPATH}/src/github.com/portainer/portainer
  #yarn
  #yarn start
after_script:
 # - docker push $REPO
